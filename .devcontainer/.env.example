# --- Which GUI sidecars to run ---
# CSV: novnc,webtop,chrome  (or) all
GUI_PROVIDERS=webtop

# Toggle audio (Webtop). Requires HTTPS; Codespaces forwarded URLs are HTTPS.
WEBTOP_AUDIO=1

# Enable Chrome DevTools (remote debugging) by default for GUI sidecars
GUI_CHROME_DEBUG=1

# Distinct host ports to avoid collisions when running multiple GUIs
GUI_NOVNC_HTTP_PORT=6080
GUI_WEBTOP_HTTPS_PORT=3001
GUI_CHROME_HTTPS_PORT=3002

GUI_NOVNC_DEVTOOLS_PORT=9222
GUI_WEBTOP_DEVTOOLS_PORT=9223
GUI_CHROME_DEVTOOLS_PORT=9224

# Basic auth for Webtop/Chrome (keep forwarded ports PRIVATE in Codespaces)
WEBTOP_USER=dev
WEBTOP_PASSWORD=change-me
CHROME_USER=${WEBTOP_USER}
CHROME_PASSWORD=${WEBTOP_PASSWORD}

# Locale/TZ
DEFAULT_TZ=Europe/Dublin
DEFAULT_LOCALE=en_IE.UTF-8
DEFAULT_LANGUAGE=en_IE:en

# --- Supabase include-list UX (comma-separated) ---
# Example: db,auth,rest,realtime,storage,studio
SUPABASE_INCLUDE=db,auth,rest,realtime,storage,studio
