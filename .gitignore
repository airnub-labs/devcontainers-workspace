# Ignore everything at the repo root (files & folders)
 /*
# Also ignore all dotfiles at root
 /.*

# Allow-list just these:
!.gitignore
!.gitattributes
!*.code-workspace

# Allow the shared devcontainer config (and everything in it)
!.devcontainer/
!.devcontainer/**
/.devcontainer/.env
/.env
!supabase/
!supabase/**
/supabase/docker/volumes/

# (Optional) keep these if you use them:
!README.md
!.github/
!.github/**
!scripts/
!scripts/**

*.DS_Store
.airnub-current-project
.pnpm-store
